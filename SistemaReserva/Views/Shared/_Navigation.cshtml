<nav class="navbar navbar-expand-lg navbar-dark bg-primary sticky-top shadow">
    <div class="container">
        <a class="navbar-brand" href="@Url.Action("Index", "Home")">
            <i class="fas fa-hotel me-2"></i>Luxury Palace
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="@Url.Action("Index", "Home")">Inicio</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Index", "Habitacion")">Habitaciones</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="@Url.Action("Index", "Reserva")">Reservas</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#contacto">Contacto</a>
                </li>
            </ul>
            <div class="ms-3">
                <a href="@Url.Action("Crear", "Reserva")" class="btn btn-outline-light">
                    <i class="fas fa-calendar-check me-1"></i> Reservar Ahora
                </a>
            </div>
        </div>
    </div>
</nav>